{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@monorepo/typescript-config/tsconfig.node.json",
  "include": [
    // Almost all files in packages/ and samples/ are covered by separate configs
    // So we only include top-level files that are not already covered.
    "*.ts"
  ]
}
